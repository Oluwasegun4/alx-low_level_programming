#include "main.h"
/**
* mul - multiplies two integers
* @a: one integer to be multiplied
* @b: second integer to be multiplied
* Return: Always return the result of the mult
*/

int mul(int a, int b)
{
	int m;

	m = a * b;
	return (m);
}
